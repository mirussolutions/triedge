<div class="row">
   <div class="col-md-8">
     <ul class="breadcrumb">
     <li> <%= link_to 'Home', admin_dashboard_path %></li>
         <li> <%= link_to 'Courses', admin_chapters_path %></li>
         <li> <%= link_to @chapter.course.title, admin_course_path(@chapter.course_id) %></li>
         <li> <%= link_to @chapter.title, admin_chapter_path(@chapter.id) %></li>
           <li class="active">Edit Quiz</li>
    </ul>
   </div>
   </div>  

  <div class="row">
   <div class="col-md-8">
      <h1>Edit Quiz</h1>
  <div class="panel panel-default">
  <div class="panel-body">
      <%= form_for([:admin, @chapter, @quiz], html: { class: "form-horizontal" }) do |f| %>
     <% if @quiz.errors.any? %>
    <div class="alert alert-warning">

        <a href="#" class="close" data-dismiss="alert">&times;</a>
        <strong>Warning!</strong>
         <% @quiz.errors.full_messages.each do |message| %>
         <li><%= message %></li>
         <% end %>
    </div>
     <% end %>

     <div class="form-group">
        <label for="title" class="col-sm-2 control-label">Quiz Title</label>
      <div class="col-sm-10">
        <%= f.text_field :title, :class=>"form-control", :id=>"title"  %>
      </div>
     </div>
      <div class="form-group">
      <div class="col-sm-10">
        <%= f.submit 'Update Quiz', :class=>"btn btn-success btn-lg" %>
      </div>
      </div>
    <% end %>
  </div>
  </div>
  </div>
</div>
</div>
